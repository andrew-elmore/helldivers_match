
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#fce84f',
      light: '#ffff81',
      dark: '#c7b819',
    },
    secondary: {
      main: '#000000',
      light: '#484848',
      dark: '#000000',
    },
  },
});

export default theme;
